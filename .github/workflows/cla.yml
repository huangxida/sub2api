name: "CLA Assistant"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: CLA disabled for fork sync PRs
        run: echo "CLA Assistant is disabled in this fork."
